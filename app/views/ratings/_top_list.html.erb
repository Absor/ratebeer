<table class="table table-hover">
  <thead>
  <tr>
    <th>Name</th>
    <th>Average</th>
  </tr>
  </thead>
  <% top.each do |item| %>
      <tr>
        <td><%= link_to item, item %></td>
        <td><%= round(item.average_rating) %></td>
      </tr>
  <% end %>
</table>